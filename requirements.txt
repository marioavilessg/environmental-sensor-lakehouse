# Dependencias Python del proyecto IoT Medallion.
# El entorno principal se levanta con Docker Compose; este fichero documenta
# las librerias usadas por los scripts y sirve como respaldo para ejecucion local.

# Pipeline PySpark y validacion
pyspark==3.5.0
PyYAML>=6.0.1
boto3>=1.34.0
s3fs>=2024.2.0
fsspec>=2024.2.0
hdfs>=2.7.3

# Captura desde Arduino/ESP32 por puerto serie
pyserial>=3.5

# Publicacion y consulta analitica para Superset/DuckDB
duckdb>=0.10.0
duckdb-engine>=0.12.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9

# Utilidades disponibles en el contenedor de trabajo
polars>=0.20.0
